//GLOBALS with no windows

/*
__dirname - path to the (current) directory
__filename - path to the current file
require - function to use modules(CommonJS)
process - infromation about env(enviromental variables) where the program is being executed
module - infromation about the current module(file)
*/

console.log(__dirname);
console.log(__filename);
console.log(module);
// setInterval(() => {console.log("LISTEN TO MY HEARTBEAT, BOOM BOOM")}, 1000);